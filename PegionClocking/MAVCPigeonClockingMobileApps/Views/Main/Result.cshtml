<div data-role="view" id="FacilityView" data-title="Facilities" data-reload="true">
    <div data-role="head">
        <div data-role="navbar">
            <a data-click="redirectToRacePage" data-role="button" data-align="left">
                @Content.ButtonIcon("home")
                <span>Race Page</span></a>
            <div class="logo" />
            <span>&nbsp;</span>
        </div>
    </div>
    <div class="pagetitle">
        FORECAST DETAILS</div>
    <ul data-role="listview" data-source="foo" data-pull-to-refresh="true" data-append-on-refresh="true"
        data-template="foo-template">
    </ul>
    @CommonFooter.RollingFooter(Url)
</div>
<script type="text/x-kendo-template" id="foo-template">
   <span>${ID}</span>
</script>
<script>
    var i = 0;

    // datasource below is customized for demo purposes.
    var foo = new kendo.data.DataSource({
        transport: {
            read: { url: '@Url.Action("GetTestResult", "Main")' }
        },
        serverGrouping: false,
        group: "FacilityGroupContent",
        error: function (e) {
            DisplayJasonError(e);
        }
    });

    //    new kendo.mobile.Application();
</script>
